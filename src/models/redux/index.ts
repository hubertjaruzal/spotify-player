export interface actionModel {
  type: string;
  payload?: any;
};

export interface imageObject {
  height: number;
  url: string;
  width: number;
};

export interface artistObject {
  name: string;
};
